<h1><%= t('announcements.titles.edit_announcement') %></h1>

<%= render 'form', announcement: @announcement %>

<%= link_to t('announcements.buttons.show'), @announcement %> |
<%= link_to t('announcements.buttons.back'), announcements_path %>
